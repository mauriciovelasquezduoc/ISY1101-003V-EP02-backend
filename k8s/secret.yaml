# IMPORTANTE: Este archivo es una plantilla.
# Los valores reales NUNCA deben commitearse al repositorio.
# Usar: kubectl create secret generic alumnos-db-secret --from-literal=...
apiVersion: v1
kind: Secret
metadata:
  name: alumnos-db-secret
  namespace: default
type: Opaque
stringData:
  datasource-url: "jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1"
  datasource-username: "sa"
  datasource-password: ""
